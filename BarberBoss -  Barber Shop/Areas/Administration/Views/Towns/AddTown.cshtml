@model BarberBoss___Barber_Shop.ViewModels.Towns.TownInputModel
@{
    this.ViewData["Title"] = "Add Town";
}

<div class="col-lg-12">
    <h2>New Town</h2>
    <form method="post" enctype="multipart/form-data" class="pt-4">
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Name</label>
            <div class="col-sm-10">
                <input type="text" asp-for="@Model.Name" class="form-control" required="required">
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <div class="col-sm-6 text-right">
                <button type="submit" class="btn btn-success"
                        asp-area="Administration" asp-controller="Towns" asp-action="AddTown">
                    Add Town
                </button>
            </div>
            <div class="col-sm-6">
                <a class="btn btn-danger" asp-area="Administration" asp-controller="Towns" asp-action="Index">Cancel</a>
            </div>
        </div>
    </form>
</div>
